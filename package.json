{
  "name": "restya",
  "author": "Kurt Endless",
  "license": "MIT",
  "scripts": {
    "client:start": "yarn --cwd client start",
    "api:start": "yarn --cwd api start:dev",
    "format": "prettier --write {api,client,redis}/src/**/*.ts"
  },
  "devDependencies": {
    "husky": "^4.2.5",
    "prettier": "^2.0.5"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn format && git add .",
      "pre-push": "yarn --cwd api test"
    }
  }
}
